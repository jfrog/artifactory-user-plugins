# Artifactory User Plugin Metadata Sample
#
# name: Plugin Name
# description: one line description that will appear on JFrog User Plugin store
#
# tag: suggestions below
#  -package type: [maven | gradle | ivy | sbt | nuget | gems | npm | bower | debian | composer | pypi | docker | vagrant | gitlfs | yum | conan | chef | puppet | generic]
#  -function: [build | replication | storage | config | deploy | download | ha | layout | search | security | statistics | storage | xray]  
#
# art-version: minimal artifactory version
#
#
name: Build modules
description: Remove build modules information from buildinfo 
tag:
    - buildinfo
art-version: "4.14.3"
