Collection of cleanup plugins:

artifactCleanup:
Cleans up artifacts not downloaded for the last n months.
Scheduled job that can also be invoked via a REST exit point.


delete_deprecated:
Deletes artifacts marked with 'analysis.deprecated=true' property.

delete_empty_dirs:
Deletes empty directories.